/* 
1. Create two variables with the values "I am learning" and
"JavaScript". Concatenate these together using the + operator and store
the value in a new variable called result. Console log result.
*/
const var1 = "I am learning";
const var2 = "JavaScript";
const result1 = var1 + var2;
console.log(result1);

/* 
2. Complete the same task, this time using the string concat().
*/
const result2 = var1.concat(var2);
console.log(result2);
