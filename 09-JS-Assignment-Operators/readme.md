### TASKS

1. What will the value of x be in the following?
   let x = 30;
   x += 10;
   x /= 2;

2. Create a variable called xp and initialise it to have a value of 250, multiply the value by 2 and console log the value.
