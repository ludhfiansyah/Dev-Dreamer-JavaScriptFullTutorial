/**_ TASKS _**

1. Create an undefined variable called course. Use the console to
   display the course or otherwise the default text "Please select a
   course".

2. What will the following return?

   let courseProgress = 0;

   console.log(
   courseProgress ?? "Start the course"
   );
