/* 
1. Create three variables called s, d and b, and set their values to
   the texts single, double, and backtick, each one using the relevant
   type.
*/
const s = "single";
const d = "double";
const b = `backtick`;
console.log(s);
console.log(d);
console.log(b);

/* 
2. Create a variable called speech and set it's value to, Martin
   Luther King said I have a dream. Make sure to use speech marks and
   make sure there are no errors. Console log speech.
*/
const speech = 'Martin Luther King said "I have a dream".';
console.log(speech);

/*
3. Create a variable called tip and set it's value to We can escape
   quotes by using \. Console log tip. 
*/
const tip = `We can escape quotes by using \\.`;
console.log(tip);
